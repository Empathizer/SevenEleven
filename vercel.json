{
  "buildCommand": "npm run build",
  "installCommand": "npm install --legacy-peer-deps && cd server && npm install",
  "ignoreCommand": "git diff --quiet HEAD^ HEAD ./",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
