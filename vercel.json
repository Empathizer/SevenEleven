{
  "installCommand": "npm install --legacy-peer-deps && cd server && npm install",
  "buildCommand": "next build",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/(.*)",
      "destination": "/api"
    }
  ]
}
