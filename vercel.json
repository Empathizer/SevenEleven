{
  "buildCommand": "next build",
  "installCommand": "npm install --legacy-peer-deps && cd server && npm install",
  "framework": "nextjs",
  "rewrites": [
    {
      "source": "/api/:path*",
      "destination": "/api"
    }
  ]
}
